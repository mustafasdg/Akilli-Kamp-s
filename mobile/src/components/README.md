# components/

Tekrar kullanılabilir, **sunum odaklı (stateless/dumb)** UI bileşenleri.

Örnekler: `AnnouncementCard`, `MenuItem`, `LoadingState`, `EmptyState`, `Button`,
`ScreenContainer`. İş mantığı içermez; veriyi prop olarak alır.
