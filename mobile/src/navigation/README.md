# navigation/

React Navigation yapılandırması.

Planlanan dosyalar:
- `RootNavigator.tsx` — Auth durumuna göre `AuthStack` vs `AppTabs` seçimi.
- `AppTabs.tsx` — alt sekmeler (Duyurular, Menü, Harita, Profil).

Expo Router'a (`/app`) geçmeye karar verirsek bu klasör kaldırılır.
