# types/

Paylaşılan TypeScript tip/arayüz tanımları.

Örnekler: `models.ts` (Announcement, MenuItem, CampusLocation, User, AuthResponse),
`api.ts` (sayfalama zarfı: `PagedResult<T>` → items/page/pageSize/totalCount/...).
`any` kullanımından kaçınmak için backend kontratlarını burada modelleriz.
